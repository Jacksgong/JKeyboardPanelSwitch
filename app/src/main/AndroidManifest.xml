<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.dreamtobe.jkpswitch">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">


        <activity
            android:name=".activity.utils.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.ChattingActivity"
            android:label="@string/activity_chatting_title"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activity.JChattingActivity"
            android:label="@string/activity_jchatting_title"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activity.utils.TranslucentActivity"
            android:theme="@style/TranslucentFullSceenTheme" />


    </application>


</manifest>
